#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std; // Corrected the typo

void playGame() {
    srand(static_cast<unsigned int>(time(nullptr))); // Seed the random number generator
    int secretNumber = rand() % 100 + 1; // Random number between 1 and 100
    int guess = 0, attempts = 0;

    cout << "🎉 Welcome to the Number Guessing Game! 🎉\n";
    cout << "I've picked a number between 1 and 100. Can you guess it?\n";

    while (true) {
        cout << "\nEnter your guess: ";
        cin >> guess;
        attempts++;

        if (cin.fail()) { // Handling invalid input
            cin.clear();
            cin.ignore(10000, '\n');
            cout << "Oops! That doesn't look like a number. Try again!\n";
            continue;
        }

        if (guess < 1 || guess > 100) {
            cout << "Hey, stick to the rules! Pick a number between 1 and 100.\n";
            continue;
        }

        if (guess > secretNumber) {
            cout << "Too high! Try something lower. 🔽\n";
        } else if (guess < secretNumber) {
            cout << "Too low! Aim a little higher. 🔼\n";
        } else {
            cout << "🎯 Bingo! You guessed it in " << attempts << " attempts.\n";
            if (attempts <= 5) {
                cout << "You're a natural! 🏆\n";
            } else if (attempts <= 10) {
                cout << "Good job! That was a solid effort. 👍\n";
            } else {
                cout << "Phew! That took some time, but you made it! 🎊\n";
            }
            break;
        }
    }
}

int main() {
    char choice;
    do {
        playGame();
        cout << "\nWant to play again? (y/n): ";
        cin >> choice;
    } while (choice == 'y' || choice == 'Y');

    cout << "Thanks for playing! See you next time. 👋\n";
    retur
